package com.fyp.HostelMate.entity.enums;

// Type of request a student submits
public enum ComplaintType {
    COMPLAINT,    // General complaint about the hostel (noise, staff, food, etc.)
    MAINTENANCE   // Something needs fixing (broken fan, leaking tap, etc.)
}
